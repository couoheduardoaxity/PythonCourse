name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: pip install fastapi uvicorn sqlalchemy pytest

      - name: Run tests
        run: pytest